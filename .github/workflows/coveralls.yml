name: Coveralls

on:
  pull_request:
    branches: [ main, develop ]
    types: [closed]
env:
  path-lcov: './Stefanini.ViaReport.Core.Tests'
  filename-lcov: 'coverage.info'
  coveralls-service-name: 'Git Hub Action'

jobs:
  build-test:
    runs-on: windows-latest
    strategy:
      matrix:
        dotnet-version: [ '6.0.x' ]

    steps:
    - uses: actions/checkout@v2
    
    - name: Setup .NET ${{ matrix.dotnet-version }}
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ matrix.dotnet-version }}

    - name: Restore dependencies
      run: dotnet restore
      
    - name: Generate coverage report
      run: dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=lcov

    - name: Save coverage file
      uses: actions/upload-artifact@master
      with:
        name: coverage-file
        path: ${{ env.path-lcov }}

  publish:
    needs: build-test
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@master
    
    - name: Load coverage file
      uses: actions/download-artifact@master
      with:
        name: coverage-file
        path: ${{ env.path-lcov }}
    
    - name: Publish coverage report to coveralls.io
      run: |
        dotnet new tool-manifest
        dotnet tool install coveralls.net --tool-path tools
        dotnet tool restore
        
        $coveralls = ".\tools\csmacnz.coveralls.exe"
        
        & $coveralls --repoToken ${{ secrets.COVERALLS_REPO_TOKEN }} --commitBranch "${{ github.ref }}" --serviceName "${{ env.coveralls-service-name }}" --lcov -i ${{ env.path-lcov }}/${{ env.filename-lcov }} --useRelativePaths --commitId ${{ github.sha }}